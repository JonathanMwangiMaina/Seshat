# Allow install scripts for packages with native dependencies
allow-scripts=true

# Suppress audit warnings for known transitive dependencies
audit-level=moderate

# Use legacy peer deps for compatibility
legacy-peer-deps=true